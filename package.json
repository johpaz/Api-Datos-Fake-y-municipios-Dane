{
  "name": "api",
  "version": "1.0.0",
  "description": "Api Profinder",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch profinder.json --port 3003",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.5.3",
    "fs": "^0.0.1-security",
    "json-server": "^0.17.3"
  }
}
